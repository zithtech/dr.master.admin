# Committed dev defaults so `npm install && npm run dev` works with no setup.
# Personal overrides belong in `.env.local`, which is gitignored.

# Relative on purpose: the Vite dev proxy forwards /api to localhost:4000,
# making requests same-origin so no CORS configuration is needed.
VITE_API_BASE_URL=/api
VITE_ENVIRONMENT=development
